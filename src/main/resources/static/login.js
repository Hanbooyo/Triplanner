// 구글 로그인 버튼 클릭 이벤트 핸들러
const googleLoginButton = document.querySelector('.login-google-button');
googleLoginButton.addEventListener('click', () => {
  // 구글 로그인 로직 구현
});

// 페이스북 로그인 버튼 클릭 이벤트 핸들러
const facebookLoginButton = document.querySelector('.login-facebook-button');
facebookLoginButton.addEventListener('click', () => {
  // 페이스북 로그인 로직 구현
});
